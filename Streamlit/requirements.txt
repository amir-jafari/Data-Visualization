# IMPORTANT: this stack is pinned to numpy 1.x.
# matplotlib, pandas and statsmodels here are compiled against the numpy 1.x C-API.
# Installing anything that pulls numpy>=2 (e.g. a bare `pip install librosa`, which
# now resolves to librosa 1.x and requires numpy>=2.1) breaks all of them with
# "ImportError: numpy.core.multiarray failed to import".
# Keep librosa at 0.10.1 and numpy below 2.

streamlit==1.28.1

# S3 access -- all app data is read from s3://dats-dl/ajafari@gwu.edu/streamlit/
boto3>=1.34

pandas==2.2.2
numpy==1.26.4     # do NOT bump to 2.x
matplotlib==3.9.2
scipy==1.13.1

# For streamlit charts plot
plotly==5.17.0
altair==5.1.2
vega_datasets==0.9.0

scikit-learn==1.5.1

# For NLP part
nltk==3.8.1
seaborn==0.13.2
textblob==0.17.1

# For remove image background
rembg==2.0.52
pooch==1.8.0

transformers==4.36.2
torch==2.1.0

keras==2.14.0
tensorflow==2.14.0
watchdog==3.0.0

# Object detection
ultralytics==8.0.207

# Data augmentation
albumentations==1.3.1

# audio part
librosa==0.10.1   # do NOT bump: 1.x requires numpy>=2.1

# linear regression
statsmodels==0.14.2

# time series
tqdm==4.66.1