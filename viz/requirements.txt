# Visualization course -- everything in viz/.
#
#     pip install -r viz/requirements.txt
#
# All of these are already on the course server, so this should install
# nothing. Unpinned on purpose: pip leaves an installed package alone unless
# you pass -U, so running this will not move versions you already have.

# the lessons are notebooks -- jupyterlab to open them, nbclient to render
# them headlessly with `python viz/run.py --all`
jupyterlab
ipykernel
nbformat
nbclient

matplotlib
numpy
pandas
seaborn                   # only for a couple of comparisons
plotly                    # interactive/plotly_basics.ipynb
altair                    # interactive/altair_basics.ipynb
networkx                  # networks/networkx_basics.ipynb
pyvis                     # networks/pyvis_interactive.ipynb
streamlit                 # project/gallery.py
